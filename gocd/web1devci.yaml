pipelines:
  mps-allowances-db:
    group: common
    label_template: "${COUNT}"
    materials:
      mps-allowances-db-git:
        git: https://github.com/ukparliament/mpsallowancesdb.parliament.uk.git
        branch: development
        auto_update: true
    stages:
      - deploy:
          jobs:
            deploy:
              tasks:
                - exec:
                    run_if: passed
                    command: make
                    arguments:
                      - deploy_to_release

environments:
  Web.DEVCI:
    pipelines:
      - mps-allowances-db